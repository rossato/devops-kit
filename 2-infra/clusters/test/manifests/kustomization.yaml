apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: clusterconf
resources:
- config-map.yaml
patches:
 - patch: |-
     op: replace
     path: /data/route
     value: {{hub (lookup "route.openshift.io/v1" "argocd" "argocd-agent-principal").spec.host hub}}
